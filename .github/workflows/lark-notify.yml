name: Lark Notification

on:
  push:
    branches:
      - main

jobs:
  notify-lark:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Get Tenant Access Token
        id: get-token
        run: |
          response=$(curl -s -X POST "https://open.larksuite.com/open-apis/auth/v3/tenant_access_token/internal" \
            -H "Content-Type: application/json" \
            -d "{\"app_id\": \"${{ secrets.LARK_APP_ID }}\", \"app_secret\": \"${{ secrets.LARK_APP_SECRET }}\"}")
          token=$(echo $response | jq -r '.tenant_access_token')
          echo "TOKEN=$token" >> $GITHUB_ENV
        env:
          LARK_APP_ID: ${{ secrets.LARK_APP_ID }}
          LARK_APP_SECRET: ${{ secrets.LARK_APP_SECRET }}

      - name: Upload Image to Lark
        id: upload-image
        run: |
          curl -s -X POST "https://open.larksuite.com/open-apis/im/v1/images" \
            -H "Authorization: Bearer ${{ env.TOKEN }}" \
            -F "image_type=message" \
            -F "image=@github_logo.png" \
            > response.json
          image_key=$(jq -r '.data.image_key' response.json)
          echo "IMAGE_KEY=$image_key" >> $GITHUB_ENV

      - name: Send Lark Notification
        run: |
          curl -s -X POST "${{ secrets.LARK_WEBHOOK }}" \
            -H "Content-Type: application/json" \
            -d '{
              "msg_type": "interactive",
              "card": {
                "header": {
                  "title": {
                    "tag": "plain_text",
                    "content": "Backend Deployment"
                  },
                  "template": "blue"
                },
                "elements": [
                  {
                    "tag": "img",
                    "img_key": "${{ env.IMAGE_KEY }}",
                    "mode": "fit_horizontal",
                    "preview": true
                  },
                  {
                    "tag": "div",
                    "text": {
                      "tag": "lark_md",
                      "content": "**🤖 Deployer**\n${{ github.actor }}"
                    }
                  },
                  {
                    "tag": "div",
                    "text": {
                      "tag": "lark_md",
                      "content": "**Service Name**\n${{ github.repository }}"
                    }
                  },
                  {
                    "tag": "div",
                    "text": {
                      "tag": "lark_md",
                      "content": "**Commit Message**\n• ${{ github.event.commits[0].message }}"
                    }
                  },
                  {
                    "tag": "action",
                    "actions": [
                      {
                        "tag": "button",
                        "text": {
                          "content": "ดู Repository 🔍",
                          "tag": "plain_text"
                        },
                        "url": "${{ github.event.repository.html_url }}",
                        "type": "primary"
                      }
                    ]
                  }
                ]
              }
            }'
        env:
          LARK_WEBHOOK: ${{ secrets.LARK_WEBHOOK }}
